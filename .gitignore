node_modules/
.next/
out/
.env*.local
.env
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Claude Code local permissions — personal, not the project
.claude/settings.local.json

# Eval history is a record of runs, not source. It grows every run and
# merging two people's copies means nothing.
evals/
